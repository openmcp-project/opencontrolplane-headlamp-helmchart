apiVersion: v2
name: headlamp-deployment
description: Headlamp deployment with plugins loaded via pluginsManager
type: application
version: 0.0.5
appVersion: "0.42.0"

dependencies:
  - name: headlamp
    version: "0.45.0"
    repository: https://kubernetes-sigs.github.io/headlamp/
